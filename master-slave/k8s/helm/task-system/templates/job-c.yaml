{{- if .Values.jobC.enabled }}
{{- range $role := list "master" "slave" }}
{{- $namespace := "" }}
{{- if eq $role "master" }}
{{- $namespace = $.Values.replication.namespaces.master }}
{{- else }}
{{- $namespace = $.Values.replication.namespaces.slave }}
{{- end }}
---
apiVersion: batch/v1
kind: Job
metadata:
  name: job-c
  namespace: {{ $namespace }}
  labels:
    app: job-c
    role: {{ $role }}
  annotations:
    # This annotation ensures job-c waits for job-b to complete
    helm.sh/hook: post-install,post-upgrade
    helm.sh/hook-weight: "2"
spec:
  ttlSecondsAfterFinished: 300
  backoffLimit: 3
  template:
    metadata:
      labels:
        app: job-c
        role: {{ $role }}
    spec:
      serviceAccountName: job-executor
      restartPolicy: OnFailure
      initContainers:
      - name: wait-for-job-b
        image: bitnami/kubectl:latest
        command:
        - sh
        - -c
        - |
          echo "Waiting for job-b to complete..."
          kubectl wait --for=condition=complete --timeout=600s job/job-b -n {{ $namespace }} || exit 1
          echo "job-b completed successfully"
      containers:
      - name: job-c
        image: "{{ $.Values.jobC.image.repository }}:{{ $.Values.jobC.image.tag }}"
        imagePullPolicy: {{ $.Values.jobC.image.pullPolicy }}
        env:
        - name: POSTGRES_HOST
          value: "postgres-service.{{ $namespace }}.svc.cluster.local"
        - name: POSTGRES_PORT
          value: "5432"
        - name: POSTGRES_DB
          valueFrom:
            secretKeyRef:
              name: postgres-secret
              key: POSTGRES_DB
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: postgres-secret
              key: POSTGRES_USER
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: postgres-secret
              key: POSTGRES_PASSWORD
        resources:
          {{- toYaml $.Values.jobC.resources | nindent 10 }}
{{- end }}
{{- end }}
